mutation Signup($input: SignupInput!) { 
    signup(input: $input) 
}

mutation Login($input: LoginInput!) { 
    login(input: $input) { 
        accessToken 
    } 
}

query Me {
    me {
        id
        email
        username
        role
    }
}